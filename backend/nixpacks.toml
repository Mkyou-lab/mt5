# Nixpacks configuration for Railway

[phases.setup]
nixPkgs = ['nodejs_20', 'npm-10_x']
aptPkgs = []

[phases.install]
dependsOn = ['setup']
cmds = [
    'npm ci --production=false'
]

[phases.build]
dependsOn = ['install']
cmds = []

[start]
cmd = 'npm start'

[variables]
NODE_ENV = 'production'
